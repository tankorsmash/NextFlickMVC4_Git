        
<div class="filter_area">
              <span>
    <form style="float: left" name="movie_search_form">

    @Html.TextBox("movie_title", "Enter a title")

    <!-- need genre, RT meter, year -->
    <select name="genre_select">
        <option value="0">All Genres</option>
        @{
            foreach (KeyValuePair<string, int> kvp in (SortedDictionary<string, int>)ViewBag.genre_dict)
            {
            <option value="@kvp.Key" >@kvp.Key </option>
            } 
        }
    </select>
    <input type="submit" value="Search Movies" />
    </form>
    @*<strong>or</strong>*@
    <form name="tag_search_form">

@*    @Html.TextBox("tag_string", "Enter a tag")
*@
    <select name="tag_string">
        <option value="0">All Tags</option>
        @{
            foreach (KeyValuePair<string, int> kvp in (SortedDictionary<string, int>)ViewBag.tag_dict)
            {
            <option value="@kvp.Key" >@kvp.Key </option>
            } 
        }
    </select>
    <input type="submit" value="Search Tags" />
    </form>
    </span>
    <span>
        <form name="movie_year_form" class="movie_year_form">
            @Html.DropDownList("year", (List<SelectListItem>)ViewBag.DropDownYears)
        <input type="submit" value="Search Years" />
        </form>
        <form name="movie_tvrating_form" class="movie_tvrating_form">
            @Html.DropDownList("tv_rating", (List<SelectListItem>)ViewBag.DropDownTvRating)
        <input type="submit" value="Search MPAA Ratings" />
        </form>
    </span>
</div>


