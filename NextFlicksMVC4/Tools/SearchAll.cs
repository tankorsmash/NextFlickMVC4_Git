using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using NextFlicksMVC4.Views.Movies.ViewModels;

namespace NextFlicksMVC4
{
    public static class SearchAll
    {/*
        public static IQueryable<FullViewModel> DetermineInputType(string searchTerm)
        {
            int intSearch;
            Int32.TryParse(searchTerm, intSearch);
            if (intSearch != null)
            {
                //check for length of int
                //either 4 or 2 length(69, 1969)
                SearchYears(intSearch);
                //1, 2 or 3 length (ie: 5, 88, 100)
                SearchTMeter(intSearch);
                //just search this all the time until i better understand what it is
                SearchMaturityRating(intSearch);
            }
            else 
            {
                SearchTitle(searchTerm);
                SearchTag(searchTerm);
                SearchGenres(searchTerm);
            }
            //Union results and return the FullViewModel
          
        }*/
    }
}